@model CursoMVC.Models.Category

@{
    ViewData["Title"] = "Detailhes";
}

<h1 class="text-center">Detailhes</h1>

<div>
    <h4>Categoria</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-outline-danger mr-2" asp-action="Edit" asp-route-id="@Model.Id">Editar</a> 
    <a class="btn btn-outline-info" asp-action="Index">Voltar</a>
</div>
